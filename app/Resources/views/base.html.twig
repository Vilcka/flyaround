<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="stylesheet" href="{{ asset('build/style.css') }}">
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="{{ path('homepage') }}">FlyAround</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('planemodel_index') }}">Planes <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('reservation_index') }}">Reservations</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('flight_index') }}">Flights</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('site_index') }}">Sites</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('review_index') }}">Reviews</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('user_index') }}">Users</a>
                </li>

            </ul>
            <span class="navbar-text mr-5">
                <a href="{{ path('fos_user_security_logout') }}" class="">Logout</a>
            </span>
            <span class="navbar-text mr-5">
                <a href="{{ path('fos_user_security_login') }}" class="">Login</a>
            </span>
            <span class="navbar-text mr-5">
            <a href="{{ path('fos_user_registration_register') }}" class="">Register</a>
            </span>
        </div>
    </nav>
            {% block body %}{% endblock %}
            {% block javascripts %}{% endblock %}
        </div>
        <script src="{{ asset('build/app.js') }}"></script>
    </body>
</html>
